# Copie para .env. Consumido em app.json > extra (ou via app.config.ts).
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
EXPO_PUBLIC_API_TIMEOUT_MS=15000
# mock | homolog | producao
EXPO_PUBLIC_AMBIENTE=mock
